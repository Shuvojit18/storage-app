export function applyThemeButton(btn){ if(!btn) return; btn.addEventListener('click',()=>{ const cur=document.documentElement.getAttribute('data-theme')==='dark'?'light':'dark'; document.documentElement.setAttribute('data-theme',cur); localStorage.setItem('theme',cur); }); const saved=localStorage.getItem('theme'); if(saved) document.documentElement.setAttribute('data-theme',saved); }
export function wireDrawer(){ const d=document.getElementById('drawer'), o=document.getElementById('overlay'), b=document.getElementById('burger'), c=document.getElementById('drawerClose'); const open=()=>{d?.classList.add('open');o?.classList.add('show')}; const close=()=>{d?.classList.remove('open');o?.classList.remove('show')}; b?.addEventListener('click',open); c?.addEventListener('click',close); o?.addEventListener('click',close); }
